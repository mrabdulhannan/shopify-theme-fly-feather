
  <div class="testimonial section-{{ section.id }}-padding">
    <div class="wblpwnkl">
        <div class="page-width">
          <h2 class="wbmatry heading text-center"><span>{{ section.settings.m_title }}</span></h2>
          <div class="rless wbdesyrd">
              {% if section.blocks.size > 0 %}
              <div class="testi rless">
                  {% for block in section.blocks %}
                      <div class="cless col-12">
                          <div class="wbtestisub text-center">
                              <ul>  
                                <li class="wbrins"><img class="img-fluid max-auto" src="{{ block.settings.image | img_url: '150x150' }}" alt="{{ block.settings.image.alt | escape }}"></li>  
                                  <li class="text-center">
                                      {% render 'icon-quote' %}
                                      <hr class="testi_hr">
                                      {% if block.settings.quote != blank %}
                                          <p>{{ block.settings.quote }}</p>
                                      {% endif %}

                                      {% if block.settings.author != blank %}
                                          <h4>{{ block.settings.author }}</h4>
                                      {% endif %}
                                      {% if block.settings.desc != blank %}
                                          <h5>{{ block.settings.desc }}</h5>
                                      {% endif %}
                                  </li>
                                
                              </ul>
                        </div>
                      </div>
                  {% endfor %}
              </div>
              {% endif %}              
          </div>
        </div>
      </div>
</div>

{%- style -%}
.section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
}
@media screen and (max-width: 991px) {
    .section-{{ section.id }}-padding {
        padding-top: {{ section.settings.padding_top | times: 0.5 | round: 0 }}px;
        padding-bottom: {{ section.settings.padding_bottom | times: 0.5 | round: 0 }}px;
    }
}
{%- endstyle -%}
  
{% schema %}
  {
    "name": "Testimonials",
    "class": "spaced-section",
    "max_blocks": 9,
    "settings": [
        {
          "type": "text",
          "id": "m_title",
          "label": "Heading",
          "default": "Testimonials"
        },
        {
          "type": "header",
          "content": "t:sections.all.padding.section_padding_heading"
        },
        {  
          "type": "range",
          "id": "padding_top",
          "min": 0,
          "max": 100,
          "step": 4,
          "unit": "px",
          "label": "t:sections.all.padding.padding_top",
          "default": 0
        },  
        {
          "type": "range",
          "id": "padding_bottom",
          "min": 0,
          "max": 100,
          "step": 4,
          "unit": "px",
          "label": "t:sections.all.padding.padding_bottom",  
          "default": 0
        }  
    ],
    "blocks": [
      {
        "type": "image",
        "name": "Image",
        "type": "quote",
        "name": "Testimonial",
        "settings": [
          {
            "type": "image_picker",
            "id": "image",
            "label": "Image"
          },
          {
            "type": "richtext",
            "id": "quote",
            "label": "Text",
            "default": "<p>Add customer reviews and testimonials to showcase your store’s happy customers.</p>"
          },
          {
            "type": "text",
            "id": "author",
            "label": "Author",
            "default": "Author's name"
          },
          {
            "type": "text",
            "id": "desc",
            "label": "sub text",
            "default": "Add Text" 
          }

        ]
      }
    ],
    "presets": [
      {
        "name": "Testimonials",
        "category": "Text",
        "blocks": [
          {
            "type": "quote"
          },
          {
            "type": "quote"
          },
          {
            "type": "quote"
          }
        ]
      }
    ]
  }
{% endschema %}
